/**
 * reserved control keys
 */
export enum EReservedControlKeys {
  c = 'c',  // on level select menu: used for toggle checkpoint
  u = 'u',  // on level select menu: used for configure player controls
  l = 'l',   // on set user controls menu: used for game pause
  p = 'p'   // during level: used for game pause
}